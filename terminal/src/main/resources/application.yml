# DataSource Config
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    #    driver-class-name: org.h2.Driver
    schema: classpath:db/schema-h2.sql
    data: classpath:db/data-h2.sql
    username: root
    password: lv1031
    #    url: jdbc:h2:mem:sql_test
    url: jdbc:mysql://127.0.0.1:3306/sql_test?characterEncoding=utf8&useSSL=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&allowMultiQueries=true

# Logger Config
logging:
  level:
    com.baomidou.mybatisplus.samples.quickstart: debug

global-config:
  # 数据库相关配置
  db-config:
    #主键类型  AUTO:"数据库ID自增", INPUT:"用户输入ID",ID_WORKER:"全局唯一ID (数字类型唯一ID)", UUID:"全局唯一ID UUID";
    id-type: auto
    insert-strategy: ignored
    update-strategy: ignored